git config --global user.name "Ashraf20"
git config --global user.email "shaikhashraf42401@gmail.com"
git config --list
git clone https://github.com/Ashraf20/djangopythonproject.git
ls
ls -a
cd djangopythonproject
git status
git add filename
git add . dot sy sb file add hojygi
git commit -m "1 file add ki ha or readme ma work kiya ha"
git push origin main
git init
git remote add origin https://github.com/Ashraf20/newrepo.git
git push -u origin main (is sy sir git push likhna sy push hojata hn)
git branch -m newname


new brach create
 git checkout -b ashraf
 git branch (is sy branch check hoga )
 git checkout -b ashraf (is sy ak branch sy dosry brach ma jaskty hn)
 git branch -d branchname (isy delete hota ha )
 jo changing krygy wo soi branch ma hoga dosry ma nhi hoga

git diff 

git merge branch name
git merge ashraf


for add rest
git reset
git reset file name

for commit reset
 
git reset head~1

aoni marzi sy pychy jana ho to

git reset (jo hash number ho wo)


